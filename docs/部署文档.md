# 工程项目管理系统 - 部署文档

## 1. 部署概述

### 1.1 系统要求

| 项目 | 要求 |
|------|------|
| 操作系统 | UOS / Windows 10+ / Linux / macOS |
| CPU架构 | ARM / x86_64 |
| Python版本 | 3.7.3 及以上 |
| 内存 | 最低512MB，推荐1GB以上 |
| 磁盘空间 | 最低100MB，推荐500MB以上 |

### 1.2 技术栈

| 组件 | 版本 | 说明 |
|------|------|------|
| Python | 3.7.3+ | 运行环境 |
| Flask | 2.3.3 | Web框架 |
| SQLite | 3.x | 数据库 |
| Bootstrap | 5.3.0 | 前端框架 |

---

## 2. 环境准备

### 2.1 Windows环境

#### 2.1.1 安装Python

1. 下载Python安装包
   - 访问 https://www.python.org/downloads/
   - 下载Python 3.7.3或更高版本

2. 安装Python
   - 双击安装包
   - 勾选"Add Python to PATH"
   - 点击"Install Now"

3. 验证安装
   ```cmd
   python --version
   pip --version
   ```

#### 2.1.2 创建虚拟环境（可选）

```cmd
# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
venv\Scripts\activate
```

### 2.2 Linux/UOS环境

#### 2.2.1 安装Python

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install python3 python3-pip python3-venv

# CentOS/RHEL
sudo yum install python3 python3-pip
```

#### 2.2.2 创建虚拟环境（可选）

```bash
# 创建虚拟环境
python3 -m venv venv

# 激活虚拟环境
source venv/bin/activate
```

### 2.3 macOS环境

#### 2.3.1 安装Python

```bash
# 使用Homebrew安装
brew install python3
```

#### 2.3.2 创建虚拟环境（可选）

```bash
# 创建虚拟环境
python3 -m venv venv

# 激活虚拟环境
source venv/bin/activate
```

---

## 3. 安装部署

### 3.1 获取项目

```bash
# 克隆项目（如果使用Git）
git clone <repository_url>
cd projext3
```

### 3.2 安装依赖

```bash
# 安装Python依赖
pip install -r requirements.txt
```

### 3.3 依赖说明

| 依赖包 | 版本 | 用途 |
|--------|------|------|
| Flask | 2.3.3 | Web框架 |
| Flask-SQLAlchemy | 3.0.5 | ORM框架 |
| Werkzeug | 2.3.7 | WSGI工具库 |
| rapidocr-onnxruntime | latest | OCR识别引擎 |
| Pillow | latest | 图像处理 |
| numpy | latest | 数值计算 |
| bcrypt | latest | 密码加密 |

### 3.4 初始化数据库

```bash
# 数据库会在首次运行时自动创建
# 或手动初始化
python -c "from app import app, db; app.app_context().push(); db.create_all()"
```

### 3.5 配置文件

系统配置位于 `app.py` 文件中：

```python
# 基础配置
app = Flask(__name__)
app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite:///projects.db'
app.config['SQLALCHEMY_TRACK_MODIFICATIONS'] = False
app.config['SECRET_KEY'] = 'your-secret-key-here'

# 上传配置
app.config['MAX_CONTENT_LENGTH'] = 16 * 1024 * 1024  # 16MB
app.config['UPLOAD_FOLDER'] = 'uploads'
```

---

## 4. 运行应用

### 4.1 开发模式运行

```bash
# 直接运行
python app.py

# 或使用Flask命令
export FLASK_APP=app.py  # Linux/macOS
set FLASK_APP=app.py     # Windows
flask run
```

### 4.2 访问应用

- 地址：http://localhost:5000
- 默认端口：5000

### 4.3 生产模式运行

#### 4.3.1 使用Waitress（Windows推荐）

```bash
# 安装Waitress
pip install waitress

# 运行
waitress-serve --host=0.0.0.0 --port=5000 app:app
```

#### 4.3.2 使用Gunicorn（Linux/macOS推荐）

```bash
# 安装Gunicorn
pip install gunicorn

# 运行
gunicorn -w 4 -b 0.0.0.0:5000 app:app
```

#### 4.3.3 使用uWSGI

```bash
# 安装uWSGI
pip install uwsgi

# 运行
uwsgi --http :5000 --wsgi-file app.py --callable app
```

---

## 5. 目录结构

```
projext3/
├── app.py                      # 主应用文件
├── requirements.txt            # 依赖清单
├── add_test_data.py            # 测试数据脚本
│
├── templates/                  # HTML模板
│   ├── base.html
│   ├── dashboard.html
│   ├── project_list.html
│   ├── project_form.html
│   ├── pending_list.html
│   ├── evaluation_form.html
│   └── settings.html
│
├── static/                     # 静态资源
│   ├── css/
│   │   ├── app.css
│   │   ├── toast.css
│   │   └── components.css
│   ├── js/
│   │   ├── app.js
│   │   ├── toast.js
│   │   ├── password-modal.js
│   │   └── confirm.js
│   └── img/
│       ├── avatars/
│       ├── icons/
│       └── photos/
│
├── data/                       # 数据目录
│   ├── scms.db                 # 数据库文件
│   └── backups/                # 备份目录
│
├── uploads/                    # 上传目录
├── files/                      # OCR测试文件
│
└── docs/                       # 文档目录
    ├── 项目架构设计文档.md
    ├── 数据库设计文档.md
    ├── 程序功能说明文档.md
    └── 部署文档.md
```

---

## 6. 数据备份

### 6.1 手动备份

1. 通过系统设置页面备份
   - 访问"系统设置"页面
   - 点击"立即备份"按钮

2. 命令行备份
   ```bash
   # 复制数据库文件
   cp data/scms.db data/backups/backup_$(date +%Y%m%d%H%M%S).db
   ```

### 6.2 自动备份

系统支持定时自动备份功能：

1. 访问"系统设置"页面
2. 开启"自动备份"开关
3. 选择备份周期（日/周/月）
4. 设置备份时间
5. 保存配置

### 6.3 数据恢复

1. 通过系统设置页面恢复
   - 访问"系统设置"页面
   - 选择备份文件或上传新文件
   - 输入管理密码验证
   - 确认恢复

2. 命令行恢复
   ```bash
   # 停止应用
   # 复制备份文件
   cp data/backups/backup_xxx.db data/scms.db
   # 重启应用
   ```

---

## 7. 安全配置

### 7.1 密钥配置

修改 `app.py` 中的密钥配置：

```python
# 生成安全密钥
import secrets
secret_key = secrets.token_hex(32)

# 配置密钥
app.config['SECRET_KEY'] = secret_key
```

### 7.2 管理密码

系统默认管理密码存储在 `app.py` 中：

```python
# 默认密码哈希（密码：admin123）
PASSWORD_HASH = '$2b$12$...'
```

修改管理密码：

```python
import bcrypt
new_password = "your_new_password"
PASSWORD_HASH = bcrypt.hashpw(new_password.encode('utf-8'), bcrypt.gensalt()).decode('utf-8')
```

### 7.3 文件上传安全

```python
# 限制上传文件大小
app.config['MAX_CONTENT_LENGTH'] = 16 * 1024 * 1024  # 16MB

# 允许的文件类型
ALLOWED_EXTENSIONS = {'png', 'jpg', 'jpeg', 'csv', 'db'}
```

---

## 8. 性能优化

### 8.1 数据库优化

```python
# 启用数据库连接池
app.config['SQLALCHEMY_ENGINE_OPTIONS'] = {
    'pool_size': 10,
    'pool_recycle': 3600,
    'pool_pre_ping': True
}
```

### 8.2 缓存配置

```python
# 可选：配置Redis缓存
app.config['CACHE_TYPE'] = 'redis'
app.config['CACHE_REDIS_URL'] = 'redis://localhost:6379/0'
```

### 8.3 静态文件缓存

```python
# 配置静态文件缓存
@app.after_request
def add_cache_headers(response):
    if request.path.startswith('/static/'):
        response.cache_control.max_age = 31536000
    return response
```

---

## 9. 监控与日志

### 9.1 日志配置

```python
import logging
from logging.handlers import RotatingFileHandler

# 配置日志
handler = RotatingFileHandler('logs/app.log', maxBytes=10240000, backupCount=10)
handler.setLevel(logging.INFO)
formatter = logging.Formatter('%(asctime)s - %(name)s - %(levelname)s - %(message)s')
handler.setFormatter(formatter)
app.logger.addHandler(handler)
```

### 9.2 健康检查

```python
@app.route('/health')
def health_check():
    return {'status': 'healthy'}, 200
```

---

## 10. 常见问题

### 10.1 端口被占用

**问题**：启动时提示端口5000被占用

**解决方案**：
```bash
# Windows：查找并结束占用端口的进程
netstat -ano | findstr :5000
taskkill /F /PID <PID>

# Linux/macOS
lsof -i :5000
kill -9 <PID>

# 或使用其他端口
python app.py --port 5001
```

### 10.2 模块导入错误

**问题**：启动时提示模块未找到

**解决方案**：
```bash
# 确保已安装所有依赖
pip install -r requirements.txt

# 或单独安装缺失模块
pip install <module_name>
```

### 10.3 数据库锁定

**问题**：操作时提示数据库锁定

**解决方案**：
- 确保没有其他进程访问数据库
- 检查数据库文件权限
- 重启应用

### 10.4 OCR识别失败

**问题**：OCR识别功能无法使用

**解决方案**：
```bash
# 确保安装了OCR依赖
pip install rapidocr-onnxruntime

# 检查图片格式和大小
# 支持格式：jpg、jpeg、png
# 大小限制：16MB
```

### 10.5 编码问题

**问题**：CSV导入/导出乱码

**解决方案**：
- 确保CSV文件使用UTF-8编码
- Windows下可使用UTF-8 with BOM

---

## 11. 升级指南

### 11.1 备份数据

升级前务必备份数据库：

```bash
cp data/scms.db data/backups/backup_before_upgrade.db
```

### 11.2 更新代码

```bash
# 拉取最新代码
git pull origin main

# 更新依赖
pip install -r requirements.txt --upgrade
```

### 11.3 数据库迁移

如果数据库结构有变化：

```bash
# 使用Flask-Migrate（如果已配置）
flask db upgrade
```

---

## 12. 生产环境部署建议

### 12.1 使用反向代理

推荐使用Nginx作为反向代理：

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /static {
        alias /path/to/projext3/static;
        expires 30d;
    }
}
```

### 12.2 使用Systemd服务（Linux）

创建服务文件 `/etc/systemd/system/project-management.service`：

```ini
[Unit]
Description=Project Management System
After=network.target

[Service]
User=www-data
Group=www-data
WorkingDirectory=/path/to/projext3
ExecStart=/path/to/venv/bin/gunicorn -w 4 -b 127.0.0.1:5000 app:app
Restart=always

[Install]
WantedBy=multi-user.target
```

启动服务：

```bash
sudo systemctl daemon-reload
sudo systemctl start project-management
sudo systemctl enable project-management
```

### 12.3 使用Docker部署

创建 `Dockerfile`：

```dockerfile
FROM python:3.9-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 5000

CMD ["gunicorn", "-w", "4", "-b", "0.0.0.0:5000", "app:app"]
```

构建并运行：

```bash
docker build -t project-management .
docker run -d -p 5000:5000 -v $(pwd)/instance:/app/instance project-management
```

---

## 13. 联系支持

如有问题，请通过以下方式获取支持：
- 查看项目文档
- 提交Issue
- 联系开发团队

---

## 14. 版本历史

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| 2.0.0 | 2025-04 | 项目操作改为模态框模式、CSS文件整合优化、代码清理优化 |
| 1.2.5 | 2025-04 | 配置文件统一管理、数据库迁移至data目录、修复路径配置问题 |
| 1.2.3 | 2025-04 | 新增页码状态保持、修复venv依赖检查、优化进程状态显示 |
| 1.2.2 | 2025-04 | 优化列表排序、默认分页、CSV导入功能 |
| 1.2.0 | 2025-04 | 新增自动备份、密码验证、分页功能 |
| 1.1.0 | 2025-03 | 新增OCR识别功能 |
| 1.0.0 | 2024 | 初始版本发布 |
